<%-include ('../../views/partials/admin/header')%>

<!-- <style>
    /* Main Content Styles */
 .main-content {
     margin-left: 250px;
     margin-top: 70px;
     padding: 2rem;
     min-height: calc(100vh - 120px);
 }

/* Responsive Design */
 @media (max-width: 768px) {
     .main-content {
         margin-left: 70px;
     }
 }

 @media (max-width: 480px) {
     .main-content {
         padding: 1rem;
     }
 }
</style>


<section class="main-content">
<form action="/admin/edit-category/<%= category._id %>" method="post">
    <button type="submit">Update Category</button>
    <label for="product_name">Name</label>
    <input type="text" name="categoryName" id="" value="<%= category.name %>">

    <label for="">Description</label>
    <textarea name="description" id=""><%= category.description %></textarea>
</form>
</section> -->

<style>
    /* Main Content Styles */
    .main-content {
        margin-left: 250px;
        margin-top: 70px;
        padding: 2rem;
        min-height: calc(100vh - 120px);
        background-color: #f8f9fa;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Form Container */
    .edit-category-form {
        max-width: 600px;
        margin: 0 auto;
        background: white;
        padding: 2rem;
        border-radius: 8px;
        box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
    }

    /* Form Title */
    .form-title {
        color: #2c3e50;
        margin-bottom: 1.5rem;
        font-size: 1.5rem;
        font-weight: 600;
        text-align: center;
    }

    /* Form Elements */
    .form-group {
        margin-bottom: 1.5rem;
    }

    .edit-category-form label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: 600;
        color: #495057;
    }

    .edit-category-form input[type="text"],
    .edit-category-form textarea {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid #ced4da;
        border-radius: 4px;
        font-size: 1rem;
        transition: all 0.3s ease;
    }

    .edit-category-form input[type="text"]:focus,
    .edit-category-form textarea:focus {
        border-color: #4a90e2;
        outline: none;
        box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.2);
    }

    .edit-category-form textarea {
        min-height: 120px;
        resize: vertical;
    }

    /* Submit Button */
    .submit-btn {
        background-color: #4a90e2;
        color: white;
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 4px;
        font-size: 1rem;
        font-weight: 500;
        cursor: pointer;
        transition: background-color 0.3s;
        display: block;
        width: 100%;
        margin-top: 1rem;
    }

    .submit-btn:hover {
        background-color: #3a7bc8;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .main-content {
            margin-left: 70px;
        }
    }

    @media (max-width: 480px) {
        .main-content {
            padding: 1rem;
        }
        
        .edit-category-form {
            padding: 1.5rem;
        }
    }
</style>

<section class="main-content">
    <form class="edit-category-form" action="/admin/edit-category/<%= category._id %>" method="post">
        <h2 class="form-title">Edit Category</h2>
        
        <div class="form-group">
            <label for="categoryName">Category Name</label>
            <input type="text" name="categoryName" id="categoryName" value="<%= category.name %>" required>
        </div>
        
        <div class="form-group">
            <label for="categoryDescription">Description</label>
            <textarea name="description" id="categoryDescription" required><%= category.description %></textarea>
        </div>
        
        <button type="submit" class="submit-btn">Update Category</button>
    </form>
</section>
<%-include ('../../views/partials/admin/footer')%>